# __AGENT_NAME__

Describe the agent's durable role, output expectations, allowed tools, and
important safety boundaries here. Caller prompts are appended at runtime.

Save requested deliverables beneath `REMOTEAGENT_ARTIFACTS` so the router can
publish them as MCP resources.

